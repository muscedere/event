{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="d-flex" style="justify-content: space-between">
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    <a class="btn btn-secondary" href="{{ path('app_home') }}">Retour à la page d'acceuil</a>
</div>
{{ form_end(form) }}
